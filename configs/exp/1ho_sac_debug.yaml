# @package _global_

wandb: false

buffer_size: 1000
batch_size: 20
reuse: 1
steps_per_rollout: 100
eval_per_rollout: 1
eval_eps: 1
eval_per_save: 1

agent_name: AgentSAC
act_net: ActorFixSAC
critic_net: CriticTwin
net_type: attn
if_act_target: false

defaults:
  - override /env: 1ho_debug